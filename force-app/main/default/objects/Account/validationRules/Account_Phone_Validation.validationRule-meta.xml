<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Account_Phone_Validation</fullName>
    <active>true</active>
    <description>Ensures the Phone field is not empty and is exactly 10 digits long.</description>
    <errorConditionFormula>OR(
  ISBLANK(Phone),
  NOT(REGEX(Phone, "[0-9]{10}"))
)</errorConditionFormula>
    <errorMessage>The Phone field is empty or not in a standard format</errorMessage>
</ValidationRule>
